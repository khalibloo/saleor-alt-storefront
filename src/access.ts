export default function(initialState) {
  //   console.log("initial", initialState);
  //   const { userId, role } = initialState;

  return {
    // role: initialState.access.role,
    // canUpdateFoo: role === "admin",
    // canDeleteFoo: foo => {
    //   return foo.ownerId === userId;
    // },
  };
}
