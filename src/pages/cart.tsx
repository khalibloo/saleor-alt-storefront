import React from "react";
import { Typography } from "antd";

const CartPage = () => {
  return (
    <div>
      <Typography.Title level={1}>My Cart</Typography.Title>
    </div>
  );
};

export default CartPage;
