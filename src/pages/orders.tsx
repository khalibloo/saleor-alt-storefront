import React from "react";
import { Typography } from "antd";

const OrdersPage = () => {
  return (
    <div>
      <Typography.Title level={1}>My Orders</Typography.Title>
    </div>
  );
};

export default OrdersPage;
